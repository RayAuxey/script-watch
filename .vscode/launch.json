{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program",
            "program": "${workspaceFolder}/test",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}
